# DraftMD Built-in Theme Sources

DraftMD ships with twelve built-in themes. The standalone CSS files in this directory mirror those bundled themes for development and review; the macOS product does not expose custom-theme import UI.

Theme colors are checked with:

```bash
npm run check:theme-colors
```
